name: flutter_test_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  # Локальное сохранение задач между запусками приложения.
  shared_preferences: ^2.5.5
  # Публикация задач в общий App Group-контейнер и обновление iOS-виджета.
  home_widget: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
